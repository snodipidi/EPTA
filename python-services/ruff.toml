# Единый линт/формат для всего дерева Python-сервисов EPTA.
target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "I", "B", "UP", "ASYNC", "BLE"]

[lint.per-file-ignores]
# В тестах допускаем assert и длинные строки фикстур.
"**/tests/**" = ["S101"]
